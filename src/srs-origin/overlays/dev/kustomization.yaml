apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev-ingest
commonLabels:
  app: srs-origin
  group: ingest
  env: dev

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: srs-origin-conf-dev
    files:
      - conf/srs.conf

resources:
  - ../../base

patchesStrategicMerge:
  - add-dev-config.yaml
