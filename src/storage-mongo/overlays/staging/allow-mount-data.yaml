apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: storage-mongo
spec:
  template:
    spec:
      containers:
        - name: mongo
          volumeMounts:
            - name: data
              mountPath: /data/db
      volumes:
        - name: data
          hostPath:
            path: /usr/staging-mongo
