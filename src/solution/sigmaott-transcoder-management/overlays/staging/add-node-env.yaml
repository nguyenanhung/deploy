apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-transcoder
spec:
  template:
    spec:
      containers:
        - name: api-transcoder
          env:
          - name: NODE_ENV
            value: staging