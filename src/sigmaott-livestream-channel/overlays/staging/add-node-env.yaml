apiVersion: apps/v1
kind: Deployment
metadata:
  name: livestream-channel
spec:
  template:
    spec:
      containers:
        - name: livestream-channel
          env:
          - name: NODE_ENV
            value: staging